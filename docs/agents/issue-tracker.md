# 工作项

当前实现任务保存在 `.trellis/tasks/`，迁移验收在 `docs/migration-parity.md`。仓库远端为 `Atingaii/Vela`；如用户明确要求发布 Issue，使用该仓库 GitHub Issues，多行内容写临时文件并通过 `gh issue create --body-file` 提交。

初始化不创建远程 Issue、评论或标签。外部 PR 不作为自动需求入口。尚未安装 triage，因此不创建 triage 标签。
